pub fn dense_ranking(ranked_scores: Vec<i32>, last_player_next_scores: Vec<i32>) -> Vec<i32> {
    println("There are {} ranked players. list={}", ranked_scores.len(), ranked_scores);
    
    
    for i in 1..last_player_next_scores.len() {

    }


    let player_ranking = Vec(1);
    return player_ranking;
}